<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bradley.codebreakers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HardMakeCode"></activity>
        <activity android:name=".MediumMakeCode" />
        <activity android:name=".EasyMakeCode" />
        <activity android:name=".HardVsCPU" />
        <activity android:name=".MediumVsCPU" />
        <activity
            android:name=".Result"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".EasyVsCPU" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MyService" />
    </application>

</manifest>