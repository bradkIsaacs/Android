<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.codebreakers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HardFriend"/>
        <activity android:name=".MediumFriend" />
        <activity android:name=".EasyFriend" />
        <activity android:name=".Hard" />
        <activity android:name=".Medium" />
        <activity
            android:name=".Result"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Easy" />
        <activity android:name=".MainActivity" android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".MyService"/>
    </application>

</manifest>